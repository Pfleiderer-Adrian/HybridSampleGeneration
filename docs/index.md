# Hybrid Sample Generator

This site contains a [searchable configuration reference](configuration/index.md) for every pipeline section, every registered model variant, and the fusion backend.

The reference is generated from the configuration classes. The code supplies field names, types, and defaults; short explanations live in the generator script. The CI check detects new parameters that lack an explanation.

For an introduction to the full workflow, see the [GitHub README](https://github.com/Pfleiderer-Adrian/HybridSampleGeneration#readme).

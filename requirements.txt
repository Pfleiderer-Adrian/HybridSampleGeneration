# Direct runtime dependencies of the installed hybrid_sample_generator package.
# pip installs the platform-appropriate PyTorch wheel; CUDA-specific installs may
# still follow https://pytorch.org/get-started/locally/ before installing this package.
torch>=2.2,<3
matplotlib>=3.8,<4
numpy>=1.26,<3
optuna>=4.0,<5
pandas>=2.1,<3
scikit-image>=0.23,<1
scipy>=1.12,<2
tqdm>=4.66,<5
